<%= render 'shared/pages_title', title: "showing all recipes" %>

<div align="center">
	<%= will_paginate @recipe %>
</div>

<%= render 'recipe', obj: @recipes %>

<div align="center">
	<%= will_paginate @recipe %>
</div>
